{
  "name": "backend",
  "version": "1.0.0",
  "description": "FastAPI backend for Curiosity Engine",
  "scripts": {
    "dev": "cd app && python -m uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "install": "pip install -r requirements.txt",
    "test": "pytest"
  },
  "private": true
} 