{
  "name": "curiosity-extension",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "vite build && tsc src/content-script.ts --outDir dist && npm run copy-files",
    "copy-files": "cp src/manifest.json dist/ && cp src/background.ts dist/background.js && cp -r public dist/ && cp src/popup/index.html dist/popup.html && cp src/sidepanel/index.html dist/sidepanel.html"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.1",
    "@types/chrome": "^0.0.254",
    "preact": "^10.26.6",
    "typescript": "^5.0.0",
    "vite": "^6.3.5"
  }
}