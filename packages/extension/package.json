{
  "name": "curiosity-extension",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "vite build",
    "postbuild": "rm -rf dist && mkdir dist && cp manifest.json dist/ && cp background.js dist/ && cp content-script.js dist/ && cp popup.html dist/ && cp popup.js dist/ && cp sidepanel.html dist/ && cp -R icons dist/icons || true"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.1",
    "preact": "^10.26.6",
    "vite": "^6.3.5"
  }
}