{
  "name": "curiosity-extension",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "vite build",
    "postbuild": "mv dist/src/sidepanel.html dist/sidepanel.html && rmdir dist/src || true && cp manifest.json dist/manifest.json && cp background.js dist/background.js && cp content-script.js dist/content-script.js && cp -R icons dist/icons || true"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.1",
    "preact": "^10.26.6",
    "vite": "^6.3.5"
  }
}