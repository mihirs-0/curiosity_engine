{
  "name": "curiosity-extension",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && vite build && cp manifest.json dist/ && cp -r icons dist/icons/"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.1",
    "@types/chrome": "^0.0.254",
    "preact": "^10.26.6",
    "typescript": "^5.0.0",
    "vite": "^6.3.5"
  }
}